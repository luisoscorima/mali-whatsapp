-- Esquema: la aplicación lo crea al arrancar con runMigrations (app/src/db/migrations.js).
-- Docker: no se monta este archivo en Postgres; el contenedor de la app ejecuta migraciones contra la BD vacía.
-- Referencia humana o copias manuales: ver el mismo archivo migrations.js.

SELECT 1;
