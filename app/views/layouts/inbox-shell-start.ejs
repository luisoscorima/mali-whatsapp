<!DOCTYPE html>
<html lang="es">
  <head>
    <%- include('../partials/app-head', { pageTitle, basePath, extraHeadScripts: typeof extraHeadScripts !== 'undefined' ? extraHeadScripts : null }) %>
  </head>
  <body class="page-conversations">
    <div class="wa-layout wa-layout--inbox inbox-layout-wa conversations-inbox<%= typeof shellPageClass !== 'undefined' && shellPageClass ? ' ' + shellPageClass : '' %><%= typeof layoutModifier !== 'undefined' && layoutModifier ? ' ' + layoutModifier : '' %>">
      <%- include('../partials/wa-rail', {
        basePath,
        appBaseUrl,
        activeNav: typeof activeNav !== 'undefined' ? activeNav : '',
        showAdminNav: typeof showAdminNav !== 'undefined' ? showAdminNav : false,
        requireAuth,
        currentUser,
        areaLabel
      }) %>
