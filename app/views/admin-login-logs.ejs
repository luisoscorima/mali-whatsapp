<%- include('layouts/inbox-shell-start', {
  pageTitle: 'Inicios de sesión · MALI WhatsApp',
  basePath,
  appBaseUrl,
  requireAuth,
  currentUser,
  areaLabel,
  activeNav: typeof activeNav !== 'undefined' ? activeNav : 'admin-login-logs',
  showAdminNav,
  shellPageClass: 'conversations-page',
  layoutModifier: 'conversations-inbox--detail',
  extraHeadScripts: null
}) %>
<%- include('partials/admin-sidebar', { basePath, adminSection: 'login-logs' }) %>
<section class="inbox-main" aria-label="Registro de inicios de sesión">
  <%- include('partials/admin-mobile-nav', { basePath, adminSection: 'login-logs' }) %>
  <div class="inbox-chat-header inbox-chat-header--static">
    <h1 class="inbox-chat-heading">Inicios de sesión</h1>
  </div>
  <div class="inbox-chat-body inbox-chat-body--form">
    <div class="card">
      <p class="card-lead muted" style="margin-top: 0">
        Últimos 500 accesos al panel (zona <%= typeof displayTimezone !== 'undefined' ? displayTimezone : 'America/Lima' %>).
        <strong>En línea</strong> si hubo actividad en los últimos 5 minutos; <strong>Inactivo</strong> si no hubo peticiones (p. ej. cerró el navegador sin “Cerrar sesión”).
        Solo visible para el usuario master.
      </p>
      <div class="table-wrap table-wrap--compact">
        <table>
          <thead>
            <tr>
              <th>Usuario (correo)</th>
              <th>Inicio de sesión</th>
              <th>Cierre de sesión</th>
              <th>Estado</th>
            </tr>
          </thead>
          <tbody>
            <% if (!loginLogs || loginLogs.length === 0) { %>
            <tr>
              <td colspan="4" class="muted">Aún no hay registros.</td>
            </tr>
            <% } else { %>
            <% loginLogs.forEach(function (row) { %>
            <tr>
              <td><%= row.email %></td>
              <td><%= typeof formatExportDate === 'function' ? formatExportDate(row.logged_at) : '—' %></td>
              <td>
                <% if (row.logged_out_at) { %>
                <%= typeof formatExportDate === 'function' ? formatExportDate(row.logged_out_at) : '—' %>
                <% } else { %>
                <span class="muted">—</span>
                <% } %>
              </td>
              <td><%= row.sessionStatusLabel %></td>
            </tr>
            <% }); %>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</section>
<%- include('layouts/inbox-shell-end') %>
