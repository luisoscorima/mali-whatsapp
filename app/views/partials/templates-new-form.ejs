<%- include('template-builder-form', {
  basePath,
  error,
  selectedTemplate: duplicateSourceTemplate || null,
  templateBuilderState,
  formMode: 'create',
  formAction: `${basePath}/templates/create`,
  submitLabel: duplicateSourceTemplate ? 'Crear nueva versión' : 'Enviar a revisión Meta',
  sourceTemplateId: duplicateSourceTemplate ? duplicateSourceTemplate.id : null,
  initialName,
  initialLanguage
}) %>
