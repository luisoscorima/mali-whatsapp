import { WaEmptyPane } from '@/shared/ui/shell/WaEmptyPane'

export function AttributesEmptyPane() {
  return (
    <WaEmptyPane
      heading="Atributos de contacto"
      text="Selecciona un atributo de la lista o crea uno nuevo."
    />
  )
}
